import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2e7d32", // green (agriculture vibe)
    },
    secondary: {
      main: "#f9a825",
    },
  },
});

export default theme;
